/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTimeGeneratePipePipe } from './DateTimeGeneratePipe.pipe';

describe('Pipe: DateTimeGeneratePipee', () => {
  it('create an instance', () => {
    let pipe = new DateTimeGeneratePipePipe();
    expect(pipe).toBeTruthy();
  });
});
